# Polynomials

`MultiCheb` and `MultiPower` wrap a coefficient tensor so a polynomial can be handed to
[`solve`](@ref) directly, instead of being approximated from a callable.

```@docs
MultiCheb
MultiPower
eval_MultiCheb
eval_MultiPower
```
